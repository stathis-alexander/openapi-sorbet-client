openapi: 3.0.3
info:
  title: Minimal Pet
  version: "0.1.0"
servers:
  - url: https://example.com/v1
paths: {}
components:
  schemas:
    Pet:
      type: object
      required: [id, name]
      properties:
        id:
          type: integer
        name:
          type: string
        tag:
          type: string
          nullable: true
